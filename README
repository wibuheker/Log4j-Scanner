## Log4j Scanner
## Installation
Required: Python 3.x
```
git clone https://github.com/wibuheker/Log4j-Scanner
cd Log4j-Scanner
pip install -r requirements.txt
```
## Run
```
python run.py list.txt
```

dont forget change SERVER with ngrok tcp if u using vps you can change  TCPServer(('localhost', 9999) localhost to 0.0.0.0 and then in SERVER variable change to VPS_IP:PORT example 103.xxx.xxx.xxx:9999
